#! /usr/bin/env sh
echo "!!!!! Only use for the VERY VERY first time with empty DB, just after 'alembic upgrade head' created all the tables !!!!"
#set -e

#export PYTHONPATH=$PYTHONPATH:${PWD}
#poetry run python app/initial_data.py